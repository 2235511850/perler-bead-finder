---
AIGC:
  ContentProducer: '001191110102MAD55U9H0F10002'
  ContentPropagator: '001191110102MAD55U9H0F10002'
  Label: '1'
  ProduceID: 'b6650f73-969a-4065-95c8-5b434bee554d'
  PropagateID: 'b6650f73-969a-4065-95c8-5b434bee554d'
  ReservedCode1: 'b4c37bd6-7f42-404f-8f99-1e078f1127fd'
  ReservedCode2: 'b4c37bd6-7f42-404f-8f99-1e078f1127fd'
---

# 移动端连接指南

## 前置要求

- 电脑已安装 Python（终端运行 `python --version` 能输出版本号）
- 手机和电脑在同一局域网下

## 方式一：手机连电脑热点（推荐）

无 AP 隔离问题，连接最稳定。

### 1. 电脑开启移动热点

设置 → 网络和 Internet → 移动热点 → 打开

### 2. 手机连接热点

用手机 Wi-Fi 连上电脑发出的热点。

### 3. 查看电脑热点 IP

电脑终端运行：

```powershell
ipconfig
```

找到 **本地连接\* 2** 那张网卡的 IPv4 地址，通常是：

```
192.168.137.1
```

### 4. 启动服务器

```powershell
cd D:\project\perler-bead-finder
python -m http.server 8000 --bind 0.0.0.0
```

看到以下输出表示启动成功：

```
Serving HTTP on 0.0.0.0 port 8000 (http://0.0.0.0:8000/) ...
```

### 5. 放行防火墙（首次需要）

首次运行时，Windows 防火墙会拦截外部连接，需添加放行规则：

```powershell
Start-Process powershell -Verb RunAs -ArgumentList '-Command', 'netsh advfirewall firewall add rule name="Python HTTP 8000" dir=in action=allow protocol=TCP localport=8000'
```

会弹出 UAC 确认框，点"是"即可。此规则只需添加一次，之后不用重复。

### 6. 手机访问

手机浏览器打开：

```
http://192.168.137.1:8000/
```

### 7. 停止服务器

在电脑终端按 `Ctrl + C`。

---

## 方式二：手机和电脑连同一 WiFi

适用于公司或家庭 WiFi，但部分公司 WiFi 有 AP 隔离，可能导致手机无法访问电脑。

### 1. 查看电脑 IP

电脑终端运行：

```powershell
ipconfig
```

找到 **WLAN** 网卡的 IPv4 地址，例如：

```
10.19.155.24
```

### 2. 启动服务器

```powershell
cd D:\project\perler-bead-finder
python -m http.server 8000 --bind 0.0.0.0
```

### 3. 手机访问

手机浏览器打开：

```
http://10.19.155.24:8000/
```

> 把 IP 换成你电脑实际的 WLAN IP。

### 4. 访问不了？

如果手机提示"未连接服务器"或"无法访问"，大概率是以下原因：

| 原因 | 解决方案 |
|---|---|
| Windows 防火墙拦截 | 添加防火墙放行规则（见上方第 5 步） |
| 公司 WiFi 有 AP 隔离 | 改用方式一（手机连电脑热点） |
| 手机和电脑不在同一网络 | 确认连的是同一个 WiFi / 热点 |
| IP 地址抄错 | 重新运行 `ipconfig` 确认 |

---

## 常见问题

### Q: 局域网 HTTP 下能正常使用吗？

能。所有功能正常，唯一区别是 Service Worker 不会注册（SW 要求 HTTPS 或 localhost），无法"添加到主屏幕"作为 PWA 安装。不影响使用。

### Q: 每次都要启动服务器吗？

是的。这是本地开发调试用的临时服务器，电脑终端 `Ctrl+C` 停掉后手机就访问不了了。

### Q: 防火墙规则要每次都加吗？

不用。只需添加一次，之后 Windows 会一直放行 8000 端口。

### Q: 可以换端口吗？

可以。把命令里的 `8000` 换成其他端口（如 `3000`），手机访问地址里的端口也要对应改。注意 80 端口在浏览器中可以省略不写。

> AI生成